name: raptor
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # ==========================================================================
  # Python
  # ==========================================================================
  - python=3.10
  
  # ==========================================================================
  # Core Python packages
  # ==========================================================================
  - numpy>=1.21.0
  - pandas>=1.3.0
  - scipy>=1.7.0
  - matplotlib>=3.4.0
  - seaborn>=0.11.0
  - scikit-learn>=1.0.0
  - joblib>=1.1.0              # NEW v2.1.0 - ML model serialization
  
  # ==========================================================================
  # v2.1.0 NEW - Quality Assessment & Statistics
  # ==========================================================================
  - statsmodels>=0.13.0        # NEW v2.1.0 - Batch effect detection, ANOVA
  
  # ==========================================================================
  # v2.1.0 NEW - Resource Monitoring
  # ==========================================================================
  - psutil>=5.8.0              # NEW v2.1.0 - CPU/memory/disk monitoring
  
  # ==========================================================================
  # v2.1.0 NEW - Interactive Dashboard
  # ==========================================================================
  - streamlit>=1.28.0          # NEW v2.1.0 - Web dashboard framework
  - plotly>=5.14.0             # NEW v2.1.0 - Interactive visualizations
  
  # ==========================================================================
  # Data handling
  # ==========================================================================
  - pyyaml>=5.4.0
  - jinja2>=3.0.0
  - openpyxl>=3.0.0
  
  # ==========================================================================
  # CLI and utilities
  # ==========================================================================
  - click>=8.0.0
  - tqdm>=4.62.0
  - colorama>=0.4.4
  - colorlog>=6.6.0            # NEW v2.1.0 - Colored logging output
  
  # ==========================================================================
  # v2.1.0 NEW - Automated Reporting
  # ==========================================================================
  - markdown>=3.3.0            # NEW v2.1.0 - Markdown to HTML conversion
  
  # ==========================================================================
  # Testing and development
  # ==========================================================================
  - pytest>=7.0.0
  - pytest-cov>=3.0.0
  - black>=22.0.0
  - flake8>=4.0.0
  - mypy>=0.950
  
  # ==========================================================================
  # Documentation
  # ==========================================================================
  - sphinx>=4.0.0
  - sphinx_rtd_theme>=1.0.0
  
  # ==========================================================================
  # Alignment tools (Bioinformatics)
  # ==========================================================================
  - star>=2.7.10a
  - hisat2>=2.2.1
  - bowtie2>=2.4.5
  
  # ==========================================================================
  # Quantification tools (Bioinformatics)
  # ==========================================================================
  - rsem>=1.3.3
  - salmon>=1.9.0
  - kallisto>=0.48.0
  - stringtie>=2.2.1
  - htseq>=2.0.2
  - subread>=2.0.3             # Contains featureCounts
  
  # ==========================================================================
  # Supporting tools (Bioinformatics)
  # ==========================================================================
  - samtools>=1.15
  - bedtools>=2.30.0
  - sra-tools>=3.0.0
  
  # ==========================================================================
  # R and Bioconductor (for statistical analysis)
  # ==========================================================================
  - r-base>=4.2.0
  - bioconductor-deseq2>=1.38.0
  - bioconductor-edger>=3.40.0
  - bioconductor-limma>=3.54.0
  - bioconductor-tximport>=1.26.0
  - bioconductor-ballgown>=2.30.0
  - bioconductor-noiseq>=2.42.0
  - bioconductor-ebseq>=1.38.0
  - bioconductor-sleuth>=0.30.0
  
  # ==========================================================================
  # R packages for data handling
  # ==========================================================================
  - r-ggplot2>=3.4.0
  - r-dplyr>=1.1.0
  - r-tidyr>=1.3.0
  - r-readr>=2.1.0
  
  # ==========================================================================
  # Simulation tools
  # ==========================================================================
  - bioconductor-polyester>=1.34.0
  
  # ==========================================================================
  # Pip packages (not available in conda)
  # ==========================================================================
  - pip
  - pip:
    - bayesian-optimization>=1.2.0   # NEW v2.1.0 - Parameter optimization
    # - raptor-rnaseq>=2.1.0         # Uncomment when published to PyPI
